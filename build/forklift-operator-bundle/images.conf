#!/bin/bash -ex

# Commit message trigger to replace images
# NOTE: ECP test pipeline will fail if you use images from quay
# This is expected and is a feature, not a bug
TRIGGER="[BUNDLE-QUAY]"
echo "Event title is: ${EVENT_TITLE}"


if [[ $EVENT_TITLE == "$TRIGGER"* ]]; then 
    IMAGES="quay"; 
fi

echo "Using images from ${IMAGES}"

if [[ $IMAGES == "quay" ]]; then

IFS="@"

read -a split_image <<< $CONTROLLER_IMAGE
export CONTROLLER_IMAGE="quay.io/redhat-user-workloads/rh-mtv-1-tenant/forklift-operator-2-10/forklift-controller-2-10@${split_image[-1]}"

read -a split_image <<< $API_IMAGE
export API_IMAGE="quay.io/redhat-user-workloads/rh-mtv-1-tenant/forklift-operator-2-10/forklift-api-2-10@${split_image[-1]}"

read -a split_image <<< $VIRT_V2V_IMAGE
export VIRT_V2V_IMAGE="quay.io/redhat-user-workloads/rh-mtv-1-tenant/forklift-operator-2-10/virt-v2v-2-10@${split_image[-1]}"

read -a split_image <<< $OPERATOR_IMAGE
export OPERATOR_IMAGE="quay.io/redhat-user-workloads/rh-mtv-1-tenant/forklift-operator-2-10/forklift-operator-2-10@${split_image[-1]}"

read -a split_image <<< $POPULATOR_CONTROLLER_IMAGE
export POPULATOR_CONTROLLER_IMAGE="quay.io/redhat-user-workloads/rh-mtv-1-tenant/forklift-operator-2-10/populator-controller-2-10@${split_image[-1]}"

read -a split_image <<< $OVIRT_POPULATOR_IMAGE
export OVIRT_POPULATOR_IMAGE="quay.io/redhat-user-workloads/rh-mtv-1-tenant/forklift-operator-2-10/ovirt-populator-2-10@${split_image[-1]}"

read -a split_image <<< $OPENSTACK_POPULATOR_IMAGE
export OPENSTACK_POPULATOR_IMAGE="quay.io/redhat-user-workloads/rh-mtv-1-tenant/forklift-operator-2-10/openstack-populator-2-10@${split_image[-1]}"

read -a split_image <<< $VSPHERE_XCOPY_VOLUME_POPULATOR_IMAGE
export VSPHERE_XCOPY_VOLUME_POPULATOR_IMAGE="quay.io/redhat-user-workloads/rh-mtv-1-tenant/forklift-operator-2-10/vsphere-xcopy-volume-populator-2-10@${split_image[-1]}"

read -a split_image <<< $MUST_GATHER_IMAGE
export MUST_GATHER_IMAGE="quay.io/redhat-user-workloads/rh-mtv-1-tenant/forklift-operator-2-10/forklift-must-gather-2-10@${split_image[-1]}"

read -a split_image <<< $UI_PLUGIN_IMAGE
export UI_PLUGIN_IMAGE="quay.io/redhat-user-workloads/rh-mtv-1-tenant/forklift-operator-2-10/forklift-console-plugin-2-10@${split_image[-1]}"

read -a split_image <<< $OVA_PROVIDER_SERVER_IMAGE
export OVA_PROVIDER_SERVER_IMAGE="quay.io/redhat-user-workloads/rh-mtv-1-tenant/forklift-operator-2-10/ova-provider-server-2-10@${split_image[-1]}"

read -a split_image <<< $VALIDATION_IMAGE
export VALIDATION_IMAGE="quay.io/redhat-user-workloads/rh-mtv-1-tenant/forklift-operator-2-10/validation-2-10@${split_image[-1]}"

fi
