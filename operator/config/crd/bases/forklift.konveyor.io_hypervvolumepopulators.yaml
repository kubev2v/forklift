---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: hypervvolumepopulators.forklift.konveyor.io
spec:
  group: forklift.konveyor.io
  names:
    kind: HyperVVolumePopulator
    listKind: HyperVVolumePopulatorList
    plural: hypervvolumepopulators
    shortNames:
    - hvp
    - hvps
    singular: hypervvolumepopulator
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              diskIndex:
                description: DiskIndex is the zero-based index of this disk within
                  the VM's disk list.
                minimum: 0
                type: integer
              diskPath:
                description: |-
                  DiskPath is the full Windows path to the source VHDX file
                  (e.g. "C:\\VMs\\MyVM\\disk0.vhdx").
                type: string
              initiatorIQN:
                description: |-
                  InitiatorIQN is the IQN the copy pod must use to authenticate via the
                  target's initiator ACL (e.g. "iqn.YYYY-MM.io.forklift:copy-<migrationID>").
                type: string
              lunId:
                description: LunID is the LUN number assigned to this disk within
                  the iSCSI target.
                minimum: 0
                type: integer
              secretName:
                description: SecretName is the name of the Secret holding Hyper-V
                  provider credentials.
                type: string
              targetIQN:
                description: |-
                  TargetIQN is the iSCSI Qualified Name of the target created on the Hyper-V host
                  (e.g. "iqn.YYYY-MM.io.forklift:vm-abc123").
                type: string
              targetPortal:
                description: |-
                  TargetPortal is the iSCSI target portal address in "host:port" format
                  (e.g. "10.0.0.100:3260").
                type: string
              vmId:
                description: VMID is the unique identifier of the source VM on the
                  Hyper-V host.
                type: string
              vmName:
                description: VMName is the display name of the source VM.
                type: string
            required:
            - diskIndex
            - diskPath
            - initiatorIQN
            - lunId
            - secretName
            - targetIQN
            - targetPortal
            - vmId
            - vmName
            type: object
          status:
            properties:
              bytesTransferred:
                description: BytesTransferred is the number of bytes written to the
                  destination so far.
                format: int64
                type: integer
              phase:
                description: Phase describes the current lifecycle phase of the populator.
                type: string
              progress:
                description: Progress is a human-readable percentage string (e.g.
                  "45%").
                type: string
              totalBytes:
                description: TotalBytes is the total number of bytes expected for
                  the transfer.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
